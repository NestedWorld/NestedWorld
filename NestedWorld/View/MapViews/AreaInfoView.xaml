<UserControl
    x:Class="NestedWorld.View.MapViews.AreaInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View.MapViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="400">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="{StaticResource ForgroundBrush}"></Rectangle>
        <Image Source="{Binding Path=Image}"/>
        <Rectangle>
            <Rectangle.Fill>
                <SolidColorBrush Color="#3F616668"/>
            </Rectangle.Fill>
        </Rectangle>

        <TextBlock Text="Binding Path=Name}" Foreground="{StaticResource BackrgoundBrush}" FontSize="20" Margin="10"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <GridView >
                    
                </GridView>
            </StackPanel>
        </ScrollViewer>        
        
    </Grid>
</UserControl>

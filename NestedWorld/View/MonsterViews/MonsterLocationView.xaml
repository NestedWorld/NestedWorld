<UserControl
    x:Class="NestedWorld.View.MonsterViews.MonsterLocationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View.MonsterViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <maps:MapControl x:Name="mapControl" MapServiceToken="{StaticResource maptoken}"/>
        <Grid Grid.Row="1">
            <Grid.BorderBrush>
                <SolidColorBrush Color="{StaticResource BleuTwo}"/>
            </Grid.BorderBrush>
            <Grid.Background>
                <SolidColorBrush Color="{StaticResource foreground}"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="100*"/>
                <ColumnDefinition Width="100*"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</UserControl>

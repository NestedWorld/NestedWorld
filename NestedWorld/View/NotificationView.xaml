<UserControl
    x:Class="NestedWorld.View.NotificationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="gridMain">
        <Ellipse Height="30" Width="30" Visibility="{Binding visible}">
            <Ellipse.Fill>
                <SolidColorBrush Color="{StaticResource background}"/>
            </Ellipse.Fill>
        </Ellipse>
        <TextBlock Style="{StaticResource TextBlockStyleOne}" Visibility="{Binding visible}" Text="{Binding notifNumber}" HorizontalAlignment="Center" VerticalAlignment="Center">
            
        </TextBlock>
    </Grid>
</UserControl>

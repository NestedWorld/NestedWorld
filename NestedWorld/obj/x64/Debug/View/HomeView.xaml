<UserControl
    x:Class="NestedWorld.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{StaticResource BleuOne}"/>
        </Grid.Background>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*"/>
            <ColumnDefinition Width="65*"/>
        </Grid.ColumnDefinitions>
        <Ellipse Height="125" Width="125" StrokeThickness="2" Margin="20">
            <Ellipse.Stroke>
                <SolidColorBrush Color="{StaticResource background}"/>
            </Ellipse.Stroke>
            <Ellipse.Fill>
                <ImageBrush Stretch="Uniform" ImageSource="{Binding UserImage}">
                    <ImageBrush.RelativeTransform>
                        <CompositeTransform CenterY="0.5" CenterX="0.5" ScaleX="1.5" ScaleY="1.5"/>
                    </ImageBrush.RelativeTransform>
                </ImageBrush>
            </Ellipse.Fill>
        </Ellipse>
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
            <TextBlock Margin="10" Text="{Binding UserName}" FontSize="{StaticResource PoliceSize}" FontFamily="{StaticResource Font}">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{StaticResource background}"/>
                </TextBlock.Foreground>
            </TextBlock>
            <Rectangle Height="0.5" Width="200">
                <Rectangle.Stroke>
                    <SolidColorBrush Color="{StaticResource background}"/>
                </Rectangle.Stroke>
            </Rectangle>
            <TextBlock Margin="10" Text="{Binding UserLevel}" FontSize="{StaticResource PoliceSize}" FontFamily="{StaticResource Font}">
                <TextBlock.Foreground>
                    <SolidColorBrush Color="{StaticResource background}"/>
                </TextBlock.Foreground>
            </TextBlock>
        </StackPanel>
    </Grid>

</UserControl>


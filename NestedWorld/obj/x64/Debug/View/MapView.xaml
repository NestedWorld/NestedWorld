<UserControl
    x:Class="NestedWorld.View.MapView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NestedWorld.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d">

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="15*"/>
        </Grid.RowDefinitions>
        <Rectangle >
            <Rectangle.Fill>
                <SolidColorBrush Color="{StaticResource BleuTwo}"/>
            </Rectangle.Fill>
        </Rectangle>
        <maps:MapControl Margin="5" MapServiceToken="{StaticResource maptoken}" Style="Aerial3DWithRoads">

        </maps:MapControl>
        <StackPanel Grid.Row="1">
            <Button Style="{StaticResource ButtonStyleMenu}" Margin="10">
                <Button.Content>
                    <FontIcon Glyph="&#xE003;">
                        <FontIcon.Foreground>
                            <SolidColorBrush Color="{StaticResource BleuTwo}"/>
                        </FontIcon.Foreground>
                    </FontIcon>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>

